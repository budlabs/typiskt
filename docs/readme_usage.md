**TYPISKT_CONFIG_DIR** (defaults to
`~/.config/typiskt`) and **TYPISKT_CACHE**
(defaults to `~/.cache/typiskt`) will get
created the first time **typiskt** is executed
if the directories doesn't exist.
